import React from 'react';
import styled from 'styled-components';

const Footer = () => {
    return (
        <footer>
            <div>
            / Email 
            / Contact
            / Group Name
            </div>
        </footer>
    );
};

export default Footer;